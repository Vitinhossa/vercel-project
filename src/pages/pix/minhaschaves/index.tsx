import PixMinhasChavesTemplate from '@/components/templates/pixminhaschavesTemplate';
import ContentContainer from '@/components/atoms/ContentContainer';

export default function MinhasChaves() {
  return (
    <ContentContainer>
      <PixMinhasChavesTemplate />
    </ContentContainer>
  );
}
